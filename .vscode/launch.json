{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (watch)",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/out/prod/**/*.js"],
      "preLaunchTask": "build-watch",
      "smartStep": true
    }
  ]
}
